/// Set to [true] to run the app without any Firebase connection.
/// All data is mocked locally – perfect for UI testing on a real device.
/// Set to [false] before releasing to production.
const bool kDemoMode = true;
